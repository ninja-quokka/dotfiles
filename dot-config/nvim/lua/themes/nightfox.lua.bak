-- Note: Nightfox supports the :NightfoxInteractive command
--       which enables an autocmd to auto reload the theme
--       upon changes.

vim.pack.add({
  "https://github.com/edeneast/nightfox.nvim",
})

require('nightfox').setup({
  options = {
    styles = {
      tags = "italic",
      methods = "bold",
      functions = "bold",
      keywords = "italic",
      comments = "italic",
      parameters = "italic",
      conditionals = "italic",
      virtual_text = "italic",
      NvimDapVirtualText = "italic",
      DiagnosticHint = "italic",
      types = "italic,bold",
    },
    modules = {             -- List of various plugins and additional options
      -- ...
    },
  },
  palettes = {},
  specs = {},
  groups = {},
})

vim.cmd("colorscheme carbonfox")
